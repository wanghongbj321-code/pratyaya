# 安全策略

## 支持范围

当前维护范围包括默认分支和最新发布版本中的：

- 专家包配置与 Agent 加载
- 状态 schema、Gate 授权和 override 规则
- Canvas 审计与内容映射
- 可能导致凭据、用户材料或授权状态泄露的问题

## 报告漏洞

请不要在公开 Issue 中发布未修复的安全漏洞、真实用户材料或凭据。请通过项目维护者提供的私密联系方式报告，并尽量包含：

- 受影响的版本或提交
- 可重复的最小步骤
- 预期行为与实际行为
- 可能的影响范围

维护者会确认收到报告，评估影响，准备修复并在适合公开时发布说明。若报告包含敏感信息，请先删去令牌、密钥、个人信息和业务数据。

## 凭据与外部服务

Pratyaya 不应在仓库、Skill、fixture、示例或升级包中硬编码真实 Token、API Key 或连接器凭据。发现疑似泄露时，应立即撤销或轮换凭据，并通过私密渠道报告。

---
name: swot-distill
description: 把 SWOT/TOWS 工作坊讨论产物提炼为 Markdown 资产。先做 Key Points 概览抽取（课题卡 + 因素候选 + TOWS 策略线索 + 覆盖度初判），再按用户决策做确认包生成，输出唯一事实源 SWOT-{slug}-v{N}.md。收到 SWOT Key Points 抽取请求、确认包生成请求时使用。
---

# swot-distill：SWOT/TOWS 态势分析与策略推导提炼

把 Key Points 概览与 SWOT/TOWS 分析框架结合，形成经过对齐的、唯一事实源的确认包（`modules/SWOT-{slug}-v{N}.md`）。完成标准是"经用户审阅、因素可追溯、策略可复核、可用于后续决策讨论的 SWOT/TOWS 分析资产"，不是"看起来内容丰富"。

所有 SWOT 产物必须绑定 instance slug。slug 由主 Agent 提供，必须为 kebab-case，并与 `state.swot.{slug}.slug` 一致；本 Skill 不自动生成 `default` slug。

## 定位

**分步提炼流程**：本 skill 是 Pratyaya Coach 的"SWOT 提炼与确认包生成"能力。完整的 SWOT 工作流由主 agent 编排（见 `agents/pratyaya.md`），本 skill 不编排主流程，只在被调用时执行以下两个独立 Stage：

- **Stage 1：Key Points 抽取** — 输入材料，输出 `modules/SWOT-{slug}-keypoints.md`（讨论地图，30 秒浏览）。
- **Stage 2：确认包生成** — 输入材料 + Key Points + SWOT 框架，输出 `modules/SWOT-{slug}-v{N}.md`（确认包，唯一事实源）。

调用顺序由主 agent 决定，本 skill 不强制。但 Stage 2 的输入依赖 Stage 1 的 Key Points。本 skill 不调用 Canvas 渲染、不执行 Gate 判定。

## 唯一内容边界

开始任何 Stage 前必须读取：

1. SWOT/TOWS 框架 `frameworks/swot-frame.md`：课题卡 8 字段、因素 6 字段分组、分类规则、TOWS 策略 6 字段、最小交接 5 要素、聚焦规则；
2. `references/SWOT-spec.md`：SWOT Key Points 与确认包的固定 section 定义、ID 约定、Canvas 锚点映射。

只提取用户实际讨论的内容。框架之外的方法或术语不自动成为必填项、补问项或放行条件；只有用户明确使用时，才按原话记录。

> **调用契约**：SWOT 画布执行细节与强制执行指令见 `references/SWOT-pipeline.md`。

## 引用层级（重要）

与 GC / HMW / 5W 蒸馏一致的立场——讨论的逐字稿不具备段落级权威性：同一人在讨论前后可能表达矛盾立场、口语化试探与跑题占据大量文本、真正的事实来自"确认环节达成的共识"而非某段话。引用应基于：

- **Key Points 内的 section**（如"SWOT 因素候选 - 优势"）
- **确认包自身的 section**（如"SWOT 四象限 - SWOT-F-S01"）

逐字稿从"证据"降级为"背景材料"，仅作存档，不作引用源。

## 输入与输出

| Stage | 输入 | 输出 |
|---|---|---|
| Stage 1：Key Points 抽取 | 逐字稿或用户材料（文本或文件路径） | `modules/SWOT-{slug}-keypoints.md`（第 N 轮） |
| Stage 2：确认包生成 | 逐字稿或用户材料 + Key Points + SWOT 框架 | `modules/SWOT-{slug}-v{N}.md`（确认包，唯一事实源） |

Stage 1 与 Stage 2 可独立调用，不强制串联。但 Stage 2 的输入依赖 Stage 1 的 Key Points。

Stage 2 完成后交给主 agent 触发闸门（`swot-gate`），不直接进入 Canvas 渲染。

## Stage 1：Key Points 抽取

**目标**：在 30 秒内让用户了解"这次讨论了什么、课题边界是否明确、因素覆盖度如何、缺什么"。

**触发**：主 agent SWOT 工作流对应步骤，输入为用户材料或逐字稿（已由主 agent 存档）。

**输出**：`modules/SWOT-{slug}-keypoints.md`，结构见 `references/SWOT-pipeline.md` 的 Stage 1 模板。

**约束**：

- 长度控制：每节最多 5 条因素，供 30 秒快速浏览。
- 不做确认包生成、不评估 Gate、不写策略取舍结论。
- 末尾必须输出用户决策提示。
- 跨轮次覆盖：第 N 轮 Key Points 覆盖第 N-1 轮（同文件覆盖式更新，保留最后一轮）。
- 覆盖度初判必须包含课题卡核心边界 + 四象限 + TOWS 四组。

## Stage 2：确认包生成

**目标**：将 Key Points 转化为经过对齐的、唯一事实源的确认包 `SWOT-{slug}-v{N}.md`。

**触发**：主 agent SWOT 工作流对应步骤（用户回复"提炼"后调用）。

**输入**：

- 逐字稿或用户材料（已存档）
- `modules/SWOT-{slug}-keypoints.md`（Stage 1 产物）
- `frameworks/swot-frame.md`（SWOT 框架）

**输出**：`modules/SWOT-{slug}-v{N}.md`，全 Markdown，结构如下。

### SWOT-{slug}-v{N}.md 确认包模板

```markdown
# SWOT 确认包 v{N}

> 画布类型：SWOT/TOWS 分析与策略画布
> 版本：v{N}（基于第 X 轮 Key Points）
> 状态：{draft / gaps_open / review_ready / confirmed / rendered}
> 生成时间：{ISO 8601 datetime，由 skill 生成时写入}
> 确认人：{待填写}
> 确认人角色（可选）：{待填写}
> 确认时间：{待填写，ISO 8601 datetime}

---

## 1. 必展项（紧凑前置）

### 1.1 一句话结论

{≤50 字，概括核心态势与策略方向}

### 1.2 对齐摘要

- 共识：x 项
- 分歧：x 项
- 决策：x 项

### 1.3 阻塞项

{如有 blocker，第一条就警示标注；无则写"无"}

### 1.4 缺口速览

- blocker：x（open / closed / accepted_risk）
- major：x（open / closed / accepted_risk）
- minor：x（open / closed / accepted_risk）

### 1.5 待确认版本

v{N}

---

## 2. 课题卡

| 字段 | 内容 | 状态 |
|---|---|---|
| 分析主体 | ... | 已确认 / 待确认 |
| 决策问题与目标 | ... | 已确认 / 待确认 |
| 业务边界 | ... | 已确认 / 待确认 |
| 时间范围 | ... | 已确认 / 待确认 |
| 比较基准 | ... | 已确认 / 待补 |
| 可控边界 | ... | 已记录 |
| 已知约束 | ... | 已记录 |
| 关键假设与分歧 | ... | 持续记录 |

> 核心四项边界（分析主体、决策问题与目标、业务边界、时间范围）必须经用户确认。比较基准可待补，依赖该基准的优势或劣势暂作候选判断。

---

## 3. SWOT 四象限

### 优势（Strengths）

| 编号 | 陈述 | 判断性质 | 内容来源 | 证据与局限 | 对决策的影响 | 重要性 | 处理 |
|---|---|---|---|---|---|---|---|
| SWOT-F-S01 | ... | 事实/判断/假设 | 用户材料/补充/AI推导 | 支持材料、类型、日期、局限 | 如何影响目标、条件、基准 | 高/中/低+理由 | 保留/待验证/排除+原因 |

### 劣势（Weaknesses）

| 编号 | 陈述 | 判断性质 | 内容来源 | 证据与局限 | 对决策的影响 | 重要性 | 处理 |
|---|---|---|---|---|---|---|---|
| SWOT-F-W01 | ... | ... | ... | ... | ... | ... | ... |

### 机会（Opportunities）

| 编号 | 陈述 | 判断性质 | 内容来源 | 证据与局限 | 对决策的影响 | 重要性 | 处理 |
|---|---|---|---|---|---|---|---|
| SWOT-F-O01 | ... | ... | ... | ... | ... | ... | ... |

### 威胁（Threats）

| 编号 | 陈述 | 判断性质 | 内容来源 | 证据与局限 | 对决策的影响 | 重要性 | 处理 |
|---|---|---|---|---|---|---|---|
| SWOT-F-T01 | ... | ... | ... | ... | ... | ... | ... |

> 空象限保留版式并说明原因。因素编号不随象限调整而改变。来源不等于证明，判断性质、内容来源与证据类型分开记录。

---

## 4. 待分类候选

| 编号 | 陈述 | 归属未明原因 | 判断性质 | 内容来源 | 证据与局限 | 处理建议 |
|---|---|---|---|---|---|---|
| CAND-01 | ... | ... | ... | ... | ... | ... |

> 归属未明的因素保留候选身份，不作为分类已成立的 TOWS 依据。

---

## 5. 被排除因素

| 编号 | 陈述 | 排除原因 |
|---|---|---|
| EXCL-01 | ... | ... |

> 被排除因素保留记录和原因，便于追查策略依据。

---

## 6. TOWS 策略

### SO 策略（利用优势抓住机会）

| 编号 | 名称 | 关联因素 | 具体动作 | 作用机制 | 成立条件 | 代价与风险 | AI 推荐 | 用户决定 | 验证与复审 |
|---|---|---|---|---|---|---|---|---|---|
| SWOT-STR-SO01 | ... | SWOT-F-S01, SWOT-F-O01 | ... | ... | 假设+资源+依赖 | ... | ... | 待决策/采用/条件采用/先验证/暂缓/放弃 | ... |

### ST 策略（利用优势应对威胁）

| 编号 | 名称 | 关联因素 | 具体动作 | 作用机制 | 成立条件 | 代价与风险 | AI 推荐 | 用户决定 | 验证与复审 |
|---|---|---|---|---|---|---|---|---|---|
| SWOT-STR-ST01 | ... | ... | ... | ... | ... | ... | ... | ... | ... |

### WO 策略（克服劣势抓住机会）

| 编号 | 名称 | 关联因素 | 具体动作 | 作用机制 | 成立条件 | 代价与风险 | AI 推荐 | 用户决定 | 验证与复审 |
|---|---|---|---|---|---|---|---|---|---|
| SWOT-STR-WO01 | ... | ... | ... | ... | ... | ... | ... | ... | ... |

### WT 策略（克服劣势应对威胁）

| 编号 | 名称 | 关联因素 | 具体动作 | 作用机制 | 成立条件 | 代价与风险 | AI 推荐 | 用户决定 | 验证与复审 |
|---|---|---|---|---|---|---|---|---|---|
| SWOT-STR-WT01 | ... | ... | ... | ... | ... | ... | ... | ... | ... |

> 不强行各出一类策略；无可靠策略时说明原因。策略必须关联具体因素编号，写明作用机制与成立条件。假设沿推导链保留。AI 推荐与用户决定分开记录。

---

## 7. 选项比较与取舍

| 比较维度 | 选项 A | 选项 B | 选项 C |
|---|---|---|---|
| 收益 | ... | ... | ... |
| 代价 | ... | ... | ... |
| 可行性 | ... | ... | ... |
| 不确定性 | ... | ... | ... |
| AI 推荐 | ... | ... | ... |
| 用户决定 | ... | ... | ... |
| 理由 | ... | ... | ... |

> 保留其他比较选项。不用总分自动选最优。说明收益、代价、可行性和不确定性；方案能否并行、是否争用同一资源，也需说明。

---

## 8. 最小交接与复审

| 关联策略/缺口 | 下一步事项 | 承接角色/人 | 约定时间/触发条件 | 预期交付物 | 判断标准 |
|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... |

> 只为实际需要推进的事项建立交接。人员和时间未知时明确留缺口；AI 提议的安排标为待确认。

---

## 9. 结论登记表

| ID | 结论 | 所属区 | 类型 | 共识状态 |
|---|---|---|---|---|
| SWOT-C01 | ... | 因素/策略/交接 | fact / decision / hypothesis / recommendation | 共识 / 待确认 / 争议 |

> 本表只记录"经过讨论产生的结论"；推断不写入此表。

---

## 10. 缺口表

| ID | 等级 | 所属区 | 状态 | 描述 | 缺失影响 | 最少补问 |
|---|---|---|---|---|---|---|
| SWOT-G01 | blocker/major/minor | 课题卡/因素/策略/交接 | open/closed/accepted_risk | ... | ... | ... |

> 等级定义：blocker = 使画布核心产出无法成立；major = 会显著改变分析方向或策略；minor = 不改变核心结论。
> 状态定义：open = 未关闭；closed = 已解决；accepted_risk = 用户已显式接受风险（由确认人在确认环节写入，不由本 skill 写入）。

---

## 11. 证据与推断登记

### 11.2 推断表

| ID | 推断 | 所属区 | 影响 | 状态 |
|---|---|---|---|---|
| SWOT-INF-01 | ... | 因素/策略 | ... | 待接受 / 待拒绝 |

> 推断不写入结论登记表。"待接受"或"待拒绝"由对齐检查环节写入，不由本 skill 写入。

---

### 11.1 关键证据引用

引用 Key Points / 确认包内 section（不引用逐字稿段落）：

- SWOT 因素候选：{象限} {编号}
- SWOT 覆盖度初判：{区块} {状态}
- SWOT 缺口表 G{XX}（如已在本表登记）

---

## 12. Gate 与用户决策

> **本节属于治理元数据**，由 Gate 流程与主 Agent 在用户决策后写入。
> **业务内容变化**（第 1–11 节任何字段）必须升版 + 重跑 Gate + 重新确认；**仅修改本节不触发业务版本升版**。
> 旧版本本节审计信息随旧版确认包保留，用于历史追溯。

### 12.1 Gate 建议

- `gate_recommendation`：`pending` / `pass` / `fail`
- Gate 评估时间：{ISO 8601 datetime}
- Gate 报告摘要：{见 `../swot-gate/references/SWOT-gate.md`；本字段可写评估项 PASS/FAIL 数与最关键 1–2 项摘要}

### 12.2 用户决策

- `confirmation_mode`：`待决策` / `gate_pass` / `override`
- `render_authorized`：`false` / `true`
- 确认人：{用户填写}
- 确认人角色（可选）：{用户填写}
- 确认时间：{ISO 8601 datetime}

### 13.3 Override 审计（仅 `confirmation_mode=override` 时填写）

| Gate 项 ID | 来源 ID | 分类 | 风险等级 | 影响 |
|---|---|---|---|---|
| SWOT-GATE-0X | SWOT-{section} | business_risk | low / medium / high | ... |

> - **Override 理由**：{用户填写的影响确认 + 业务上下文}
> - **补救措施**：{用户填写的后续动作与验收条件}
> - **核心约束**：仅 `category=business_risk` 项可进入 override；`information_integrity` 失败不接受 override，必须返回补问或修订。
> - **审计完整性**：`override_audit.items` 非空且每项 `category=business_risk`，否则 Canvas 渲染前置检查阻断。
```

**约束**：

- 引用格式：仅引用 Key Points 与确认包自身的 section，**不引用逐字稿段落**。
- 推断必须独立登记，不写入结论登记表。
- 缺口必须说明"缺失影响"，不能只说"信息不足"。
- 不调用 Canvas 渲染、不执行 Gate 判定。
- 状态写入由主 agent 在用户确认后执行，不在本 skill 内部。
- 版本号管理：升版时 vN → vN+1，旧版本归档为 `modules/SWOT-{slug}-v{N}.md.previous`，不清空。
- **缺口表必须含 `状态` 列**（`open` / `closed` / `accepted_risk`），其中 `accepted_risk` 由确认人在确认环节写入，不由本 skill 写入。
- **元数据必须包含**：`画布类型` / `版本` / `状态` / `生成时间`（ISO 8601 datetime，skill 生成时自动写入） / `确认人` / `确认人角色（可选）` / `确认时间`（用户填写）。
- **第 3 节"SWOT 四象限"**是 Canvas 渲染 `swot-quadrant-s` / `swot-quadrant-w` / `swot-quadrant-o` / `swot-quadrant-t` anchor 的事实源，必须在 Stage 2 中基于证据填写，不得留空或写泛化描述（空象限需说明原因）。
- **第 12 节"Gate 与用户决策"由 Gate 流程与主 Agent 在用户决策后写入**；本 skill 不写治理结果，只负责预留该节结构。

## 升版边界

确认包版本受两类写入影响：

| 写入范围 | 是否触发升版 | 是否重跑 Gate | 是否重置授权 |
|---|---|---|---|
| 第 1–11 节业务内容（含课题卡、因素、策略、交接、结论、缺口、推断、证据登记）变化 | **是**（vN → vN+1） | **是** | **是**（清空 `gate_recommendation` / `render_authorized` / `confirmation_mode` / `override_audit`） |
| 仅第 12 节"Gate 与用户决策"治理元数据写入（Gate 报告、用户决策、Override 审计） | **否**（保留 vN） | 否（已是当前评估结果） | 否（这是当前版本的授权写入） |

**规则**：

- **业务内容变化**：必须 `version + 1` + `gate_recommendation=pending` + `render_authorized=false` + `confirmation_mode=null` + 清空当前版本 `override_audit`；旧版本确认包归档为 `SWOT-{slug}-v{N}.md.previous`，旧版第 12 节审计随旧版保留。
- **治理元数据写入**（仅第 12 节）：不触发升版；Gate 报告摘要与用户授权属于当前版本的元数据补充。
- **历史版本审计**不得清空：旧版 `SWOT-{slug}-v{N}.md.previous` 的第 12 节（包括历史 override 审计）必须完整保留，用于追溯。

## 元数据生成时间字段

- `生成时间`（`SWOT-{slug}-v{N}.md` 顶部）由本 skill 在 Stage 2 生成确认包时按系统真实时间写入（ISO 8601 datetime）。
- `确认时间`由主 Agent 在用户决策后写入；不得使用 skill 生成时间。
- 禁止在文件名、文档标题、报告标识中编造时间戳。

## 质量红线

1. 不编造、不拔高、不抹平冲突。
2. 课题卡核心四项边界必须经用户确认；未确认时先补问，不静默替用户确定边界。
3. 因素分类以分析主体为准；归属未明保留为待分类候选，不强行分类。
4. 每条因素必须有证据与局限记录；证据缺失标缺口，不得补写。
5. 行动方案不能直接当作机会。
6. TOWS 策略必须关联具体因素编号，写明作用机制与成立条件；假设沿推导链保留。
7. AI 推荐与用户决定分开记录；用户尚未选择时保留待决策，不由 AI 代选。
8. 推断必须独立登记（§11），不得混入结论或 Canvas section。
9. 不引用逐字稿段落；引用应基于 Key Points / 确认包内的 section。
10. 未完成人工确认前，不视为提炼完成；状态由主 agent 在用户确认后写入。
